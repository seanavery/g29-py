jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: build
        run: |
          docker build -t g29-py -f ../../etc/Dockerfile ./